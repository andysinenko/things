spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/things_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
  liquibase:
    enabled: false
  cache:
    type: none